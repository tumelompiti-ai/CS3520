## Exercise 1 — Larger of Two Integers

Loads two integers a=15 and b=9 from .data section.
Uses bge (branch if greater or equal) to compare them.
Registers: t1 = a, t2 = b, a0 = value to print.
The branch condition is inverted compared to C++ — 
assembly exits toward print_a when a >= b is true.